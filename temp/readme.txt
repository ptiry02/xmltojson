This readme is only here so that the empty temp directory gets tracked by git and pushed to repo.

The xml file from frontend wil be temporarly stored here until convesion is completed and will then be removed.